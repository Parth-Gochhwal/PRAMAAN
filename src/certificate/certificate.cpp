// certificate.cpp -- see include/pramaan/certificate.hpp
// FIRST TASK: primal residual check -- given a ModelIR and a candidate
// solution vector x, compute ||max(0, Ax - rhs)|| (constraint violation)
// and ||max(0, lb - x)|| + ||max(0, x - ub)|| (bound violation). This
// alone is enough for your first end-to-end "CERTIFICATE VALID" demo on
// an LP, before dual residuals or integrality checks exist.
#include "pramaan/certificate.hpp"

namespace pramaan {
// TODO
}  // namespace pramaan
