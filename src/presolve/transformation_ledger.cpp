// transformation_ledger.cpp -- see include/pramaan/transformation_ledger.hpp
//
// The TransformationLedger is header-only (all methods are inline or
// trivial). This .cpp exists only to satisfy the CMake source list and
// ensure the header compiles cleanly as a standalone translation unit.
#include "pramaan/transformation_ledger.hpp"

namespace pramaan {
// All methods are defined inline in the header.
}  // namespace pramaan
