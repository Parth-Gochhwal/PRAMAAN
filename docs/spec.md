# Where the real spec lives

Copy your finalized PRAMAAN_solution_spec.md into this file (or alongside
it) so it lives in the repo next to the code it describes. Every file in
src/ and include/ has comments referencing which "Pillar" and "Zone" of
that spec it implements -- keep them in sync as the design evolves.
